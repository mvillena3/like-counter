%h1 Like him if you want 

- if current_user
  %br/
  %br/
  %h2 Me: #{current_user.name}
  %h3
    %b My likes: 
    = current_user.likes
  %br/
  %br/

%table
  %tr
    %th Name
    %th Likes

  - @users.each do |user|
    - if current_user != user
      %tr
        %td= user.name
        %td= user.likes

%br/
= button_to 'Like', email_likes_users_path(likes: 'jb@gmail.com', liked: 'jc@gmail.com', email: true)
%br/
= link_to 'Sign up', new_user_path
|
- if signed_in?
  = link_to "Sign out", signout_path, method: 'delete'
- else
  = link_to "Sign in", new_session_path
%br
