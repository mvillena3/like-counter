%h1 Like him if you want 

- if current_user
  %br/
  %br/
  %h2 Me: #{current_user.name}
  %h3
    %b My likes: 
    = current_user.likes
  %br/
  %br/

%table
  %tr
    %th Name
    %th Likes

  - @users.each do |user|
    - if current_user != user
      %tr
        %td= user.name
        %td= user.likes
        - unless current_user == user
          %td= button_to 'Like me', likes_user_path(user.id), method: :put

%br/

= button_to 'The Real Like Button', like_path(email: true, likes: 'jb@gmail.com', liked: 'jc@gmail.com')
%br/
%br/
= link_to 'Sign up', new_user_path
|
- if signed_in?
  = link_to "Sign out", signout_path, method: 'delete'
- else
  = link_to "Sign in", new_session_path
%br
